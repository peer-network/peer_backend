extend type Query {
  moderationStats: ModerationStatsResponse!
  moderationItems(status: ModerationStatus, contentType: ModerationContentType, page: Int, limit: Int): ModerationItemListResponse!
}

extend type Mutation {
  performModeration(targetContentId: ID!, moderationAction: ModerationStatus!): DefaultResponse!
}